import { ComponentColor } from '../constants/constants';

// interface for Toast data
export default interface IToastData {
    toastIcon: ComponentColor;
    toastHeader: string;
    toastBody: string;
}